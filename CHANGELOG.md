# 변경 이력

이 파일은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을 따르고,
버전은 [유의적 버전](https://semver.org/lang/ko/)을 따릅니다(1.0.0 이전이므로 MINOR가 기능 추가·동작 변경, PATCH가 수정입니다).

## 버전 정책

- **MINOR(0.x.0)**: 새 기능 표면 추가, 사용자에게 보이는 동작 변경, 성능 특성 변경.
- **PATCH(0.0.x)**: 버그 수정, 또는 배포물(README·설정 설명)에 영향 있는 변경.
- **버전 유지**: 개발 문서만 바뀔 때(`docs/` 백로그·스펙·계획, 테스트 추가) — 사용자가 받는 것이 달라지지 않으므로 올리지 않습니다.
- 판단 기준은 "사이클이 끝났는가"가 아니라 **"사용자가 받는 산출물(.vsix)이 달라지는가"**입니다. 달라지는 사이클이 `main`에 병합될 때 **같은 사이클 안에서** 버전을 올리고 이 파일에 항목을 추가합니다 — 병합 후 별도 커밋으로 미루지 않습니다. 달라지지 않으면(위 "버전 유지") 올리지 않습니다.

## [0.24.0] — 2026-09-08

### 추가
- **새 버전이 나오면 알립니다.** 하루에 한 번 GitHub 릴리스를 확인해, 설치된 판보다 새 버전이 있으면 알림을 띄웁니다. `릴리스 노트`는 CHANGELOG의 해당 절을 편집기에서 열고, `지금 설치`는 `.vsix`를 받아 설치한 뒤 다시 로드를 권합니다. `이 버전 건너뛰기`를 누르면 그보다 더 새 버전이 나올 때까지 조용합니다. `csmscode.checkForUpdates`(기본 `true`)로 끕니다.
  - **조회에 실패하면 아무 것도 표시하지 않습니다** — 오프라인이나 GitHub에 닿지 못하는 사내망에서 알림도 오류도 뜨지 않습니다.
  - 설치는 VS Code의 내부 명령(`workbench.extensions.installExtension`)을 씁니다. 공식 확장 API가 아니라서, 그 명령이 없는 판에서는 `지금 설치` 버튼을 아예 표시하지 않고 릴리스 페이지를 여는 쪽으로 흘려보냅니다. 내려받기나 설치가 실패해도 같은 경로로 내려갑니다.
  - 내려받는 `.vsix`의 출처를 `https://github.com`으로 제한합니다 — 받은 파일을 그대로 설치하기 때문입니다.
  - 이 기능은 **GitHub에 하루 한 번 네트워크 요청을 보냅니다**(공개 저장소라 인증 없이 조회합니다). 설정으로 끌 수 있습니다.

## [0.23.2] — 2026-09-07

### 수정
- **보간이 없는 겹따옴표 문자열도 참조로 인식합니다.** PHP에서 `"local_x/card"`와 `'local_x/card'`는 같은 값이지만 파서는 다른 노드(`encapsed_string` / `string`)로 보는데, 조각들이 후자만 잡고 있었습니다. 그래서 겹따옴표로 쓴 `get_string`·`get_config`·`render_from_template`·`js_call_amd`와 `$DB` 메서드의 테이블 인자가 정의 이동·hover·하이라이트·완성·진단에서 전부 침묵했습니다. 사용처 색인은 정규식이라 처음부터 두 형태를 모두 훑고 있어서, **"사용 N건" 목록에는 나타나는데 그 자리에서 F12는 안 되는 비대칭**이 있었습니다. 실측(csms45 PHP 2,000개 파일): 새로 잡힌 팩트는 언어 문자열 +40, 테이블 참조 +27, 리터럴 대입 +134, 상수 +2건이고 **줄어든 팩트는 없습니다**. `facts()` 성능은 그대로입니다(14KB 파일 5회 표본 중앙값 5.23ms → 5.13ms).
- 보간이 섞인 문자열(`"local_{$type}/card"`)은 리터럴이 아니므로 계속 침묵합니다. 문자열 내용이 보간을 사이에 두고 조각나는데, 그 조각(`local_`)을 참조로 오인하지 않도록 문자열 내용이 **유일한 자식**일 때만 리터럴로 봅니다. 이스케이프가 섞인 문자열(`"a/b\n"`)도 같은 이유로 침묵합니다.

### 참고
- 이 변경으로 **없던 경고가 생길 수 있습니다.** `$rec = $DB->get_record("sometable", …)` 뒤의 존재하지 않는 컬럼 참조가 이제 오타 진단을 받습니다.
- 한 스코프에서 같은 `$data`를 서로 다른 테이블에 넘기는데 인용 형태가 달랐다면(`insert_record('a', $data)`와 `insert_record("b", $data)`), 전에는 하나만 보여 완성이 나왔지만 이제 둘 다 보여 모호함으로 침묵합니다.

## [0.23.1] — 2026-09-04

### 수정
- **슬래시가 든 참조의 Ctrl+호버 밑줄이 조각으로 갈리던 것을 고쳤습니다.** `render_from_template('local_ubsend/sms/send')`에서 밑줄이 `local_ubsend`·`sms`·`send` 세 토막으로 나뉘어 보였습니다. 정의 프로바이더가 대상 위치만 돌려주면 VS Code가 링크 구간을 커서 위치의 단어 범위로 추측하는데, `/`가 단어 구분자이기 때문입니다. 이제 유스케이스가 참조가 소스에서 차지하는 범위도 함께 돌려주고 프로바이더가 그 범위를 링크 구간으로 지정합니다. 템플릿·AMD·JS·mustache partial과 점이나 콜론이 든 언어 문자열 키가 대상입니다. 이동하는 위치는 이전과 같습니다.

## [0.23.0] — 2026-09-04

### 변경
- **사용처 색인이 붙잡고 있던 파일 텍스트를 없앴습니다.** 정규식으로 뽑은 조각은 V8에서 보통 원본 문자열을 가리키는 뷰라서, 열 글자짜리 키 하나를 보관해도 그 파일 전체가 메모리에 남아 있었습니다. 문자열 풀을 두어 넣을 때 사본을 만들고, 색인이 문자열 대신 id를 보관하게 해 사본을 거치지 않은 캡처는 풀에 닿을 수 없게 했습니다. 실측(csms45 24,890개 파일을 처음부터 색인하는 콜드 빌드): **창당 보유 131MB → 21.6~22.8MB**. 같은 측정에서 빌드 시간도 6.3s → 4.9s로 줄었는데, 만들어지는 가비지가 줄어든 부수 효과로 보입니다. 이 수치는 콜드 빌드 한 번의 보유량이라, 저장·재검증을 반복하는 평상시 사용 중 메모리는 이 측정이 다루지 않습니다. 스캔 결과와 조회 결과는 그대로입니다.

### 수정
- **증분 경로에서 사용처 색인의 문자열 풀이 무한정 커지는 것을 막았습니다.** 파일 삭제 이벤트는 색인 대상이 아닌 파일(예: 이미지)에도 오는데, 갱신 함수가 존재 여부와 무관하게 경로부터 풀에 인터닝해 그 경로 하나가 아무것도 가리키지 않은 채 영영 남았습니다. 이제 색인이 모르는 경로에 도장 없이 빈 텍스트가 오면(삭제 알림) 아무 일도 하지 않고, 삭제 이벤트도 저장 이벤트와 같은 색인 대상 필터를 거칩니다. 내용이 원래 빈 파일은 다릅니다 — 콜드 스캔이 읽어 도장을 찍은 파일이라 재검증 때마다 다시 읽히지 않고 도장이 그대로 유지됩니다.

### 문서
- README에서 CSMS_CORPUS 게이트 테스트 개수 표기가 엉뚱한 문장 뒤에 붙어 있던 것을 바로잡았습니다.

## [0.22.0] — 2026-09-03

### 변경
- **PHP 문법 계층을 다시 짰습니다.** 그동안 쿼리 27개가 각자 문서 전체를 훑던 것을, 조각 29개를 패턴 인덱스로 분기하는 통합 쿼리 하나로 바꿨습니다. `facts()` 실측(lib.php 14.1KB, 노드 2,561개): **17.0ms → 4.4~5.4ms**. 동작은 그대로입니다 — 배포 전 실제 Moodle 체크아웃에서 결정적 간격 표본 2,000개 파일을 이번에 제거된 옛 구현과 대조해 팩트 17종을 전수 비교했고, 불일치는 0건이었습니다.
- **1MB를 넘는 PHP 파일은 팩트 없음으로 처리합니다.** 파싱에 실패한 파일과 같은 취급으로, 그 파일의 인텔리전스만 조용히 침묵합니다.

### 수정
- **tree-sitter 런타임(`web-tree-sitter` 0.20.8 → 0.27.0)과 PHP 문법(`tree-sitter-php` 0.24.2)을 올렸습니다.** 이중 인용 문자열에 16진 이스케이프가 있으면(`"\x00"`뿐 아니라 `"\x41"` 등 전부) 파싱이 예외를 던져 그 파일의 인텔리전스가 전부 침묵하던 문제를 고쳤습니다. 문법 업그레이드로 enum·readonly 프로퍼티·네임드 아규먼트·첫클래스 콜러블처럼 이전엔 파싱 자체가 실패하던 최신 PHP 문법을 쓴 파일도 이제 파싱에 성공해, 그 파일의 인텔리전스 전체를 잃지는 않습니다. 표본 1,989개 파일 비교에서 이 변경으로 설명되지 않는 팩트 차이는 없었습니다.

## [0.21.0] — 2026-09-02

### 추가
- **`install.xml`의 `<TABLE>` 줄에서 그 테이블의 사용처를 찾습니다.** Shift+F12로 SQL 문자열의 `{tablename}`과 `$DB->insert_record('tablename', …)` 같은 DML 호출을 모두 보고, 선언 줄 위의 **"사용 N건" 버튼**으로도 같은 목록을 엽니다("모든 참조 찾기"에는 선언 위치도 함께). 설정 `csmscode.tables.codeLens`(기본 `true`).
  - 실측(hlulxp): `install.xml` 125개에 `<TABLE>` 선언 713개, 그중 **670개(94%)가 실제로 참조**됩니다. 색인에 담긴 테이블 사용처는 SQL `{name}` 13,828건 + `$DB` 리터럴 인자 후보 19,144건(그중 `sql_like` 같은 SQL 조각 헬퍼는 첫 인자가 컬럼이라 제외)입니다.
  - `{name}`은 실재 테이블인지 걸러내지 않고 담습니다 — 걸러 두면 `install.xml`에 테이블을 새로 추가했을 때 그 테이블이 "사용 0건"으로 보이기 때문입니다. 조회는 색인된 테이블 이름으로만 하므로 `{firstname}`처럼 테이블이 아닌 중괄호는 목록에 나타나지 않습니다.
  - **알려진 제한**: 테이블 이름과 **겹치는** 비SQL 중괄호는 목록에 섞입니다 — `'index.php?id={course}'`·주석 안의 SQL·PHP 문자열에 넣은 `{{user}}` 같은 것이 `course`·`user` 테이블의 사용처로 잡힙니다. 코어 테이블(`user`·`course` 등)은 사용처가 수천 건이라 목록이 매우 큽니다.
- **테이블만 대상입니다** — `<FIELD>`(컬럼)별 사용처는 범위 밖입니다.

### 변경
- 사용처 색인에 테이블 사용처가 더해져 **항목이 55,568 → 88,486건(+59%)**이 됩니다. 실측 영향(hlulxp, 웜):
  - 색인 메모리 창당 **약 100MB → 123MB**
  - 전체 스캔 4.2초 → **5~6초**
  - 캐시 파일 0.90MB → **1.09MB**, 캐시 로드 0.11초 → **0.3~0.9초**(측정 편차), 백그라운드 검증 1.2초 → **1.2~1.8초**
  - 스캔 없이 불러오는 이득은 그대로입니다(로드가 스캔의 1/5 이하).
- 캐시 형식이 v2로 바뀌었으므로 이 버전에서 처음 열 때 한 번은 전체 스캔합니다(옛 캐시 자동 폐기).
- 복원 정확성 재확인: 테이블 이름 1,433개의 사용처를 스냅샷 왕복 전수 비교해 불일치 0건.

## [0.20.0] — 2026-08-31

### 변경
- **사용처 색인 스캔이 두 배 빨라졌습니다.** 디렉터리마다 부르던 `realpath`를 심볼릭 링크로 진입할 때만 부르고(일반 하위 디렉터리는 부모 실경로에 이름을 붙여 만들어 **중복 순회 방지는 그대로** 유지), 하위 디렉터리 열거와 파일 읽기를 병렬로 합니다. 실측(hlulxp, 색인 대상 24,717개): 전체 빌드 **웜 8.5~11.2초 → 4.2~4.5초**, 콜드 53.5초 → 35.5초. 항목·위치·순서는 전과 동일합니다(적용은 파일 순서대로).

### 추가
- **사용처 색인을 디스크에 저장합니다.** 다음에 그 워크스페이스를 열면 스캔 없이 **0.1초**에 불러와 Shift+F12와 "사용 N건" 버튼이 바로 동작합니다. 불러온 뒤에는 백그라운드에서 파일 mtime·크기만 비교해(읽지 않습니다) 바뀐 파일만 다시 읽고, 반영되면 버튼·하이라이트를 다시 그립니다. 저장·삭제 뒤에도 캐시를 갱신합니다.
  - 실측(hlulxp): 전체 스캔 4,161ms → 캐시 로드 **114ms**(읽기·검증 39ms + 복원 75ms), 캐시 쓰기 187ms·0.90MB, 변경 없을 때 백그라운드 검증 1,231ms. 복원한 색인과 스캔한 색인을 네 종류 전수 비교(문자열 22,964쌍·템플릿 1,811·AMD 376·설정 952)해 불일치 0건.
  - **순회나 파일 확인이 한 건이라도 실패하면 삭제는 반영하지 않습니다.** 권한 오류·루트 일시 소실을 "파일이 사라졌다"로 오해해 색인과 캐시를 비우지 않기 위해서입니다(변경·추가는 그대로 반영).
  - 캐시를 못 믿을 때(손상·형식 이상)는 그 자리에서 버리고 전체 스캔합니다 — 캐시 때문에 참조 기능이 멈추지 않습니다.
  - 캐시 파일은 확장의 전역 저장소에 워크스페이스별로 하나(gzip 약 1MB). 형식·확장 버전이 다르거나 파일이 손상되면 조용히 버리고 전체 스캔합니다 — 추출 규칙이 바뀐 버전에서 옛 색인을 쓰지 않기 위해서입니다.
  - 설정 `csmscode.usageIndex.cache`(기본 `true`). 끄면 전처럼 매번 스캔합니다.
- 활성화 시 미리 만들지는 않습니다(여전히 첫 요청에 만듭니다) — 창마다 확장 호스트가 따로 떠서 참조를 쓰지 않는 창에도 메모리(색인당 약 100MB)를 물리게 되기 때문입니다.

### 비고
- 캐시를 불러온 직후 잠깐은 마지막 세션 기준입니다. 이 색인은 이미 "저장 시점 기준"(미저장 편집 미반영)이라 성격이 같고, 백그라운드 검증이 끝나면 맞춰집니다.
- 검증은 파일을 읽지 않고 **mtime과 크기만** 봅니다. 둘이 그대로면 변경을 알 수 없습니다 — `rsync -t`·`tar` 복원처럼 mtime을 보존하며 같은 크기로 바뀌는 경우, mtime 해상도가 1초인 파일시스템에서 같은 초 안의 동일 크기 편집이 그렇습니다(`git checkout`은 mtime을 현재 시각으로 세우므로 반영됩니다). 이럴 때는 명령 팔레트의 **"CSMS Code: 사용처 색인 다시 만들기"**로 다시 만들 수 있습니다.
- 진행률 알림은 0.4초 안에 끝나지 않는 경우에만 뜹니다 — 캐시에서 불러오는 경로(0.1초)는 조용히 지나갑니다.

## [0.19.0] — 2026-08-31

### 추가
- **`amd/src` 모듈 파일 맨 위에도 "사용 N건" 버튼(CodeLens).** mustache와 같은 방식 — 파일 전체가 하나의 모듈이라 버튼도 하나이고, 클릭하면 그 자리에서 그 모듈을 부르는 `js_call_amd` 호출처 목록이 열립니다. 설정 `csmscode.amd.codeLens`(기본 `true`).

## [0.18.0] — 2026-08-31

### 추가
- **mustache 템플릿 파일 맨 위에 "사용 N건" 버튼(CodeLens).** 파일 전체가 하나의 템플릿이므로 버튼도 하나입니다. 클릭하면 그 자리에서 Shift+F12와 같은 사용처 목록(`render_from_template` 호출·다른 mustache의 partial 참조·JS `Templates.render`)이 peek으로 열립니다. 사용처 색인이 아직 없으면 "사용 찾기"로 표시되고 클릭이 색인을 만듭니다. 테마 오버라이드 파일에서도 같은 목록입니다. 설정 `csmscode.templates.codeLens`(기본 `true`).

## [0.17.0] — 2026-08-26

### 추가
- **플러그인 설정 키 인텔리전스.** `get_config('local_x', 'key')`·`set_config('key', $v, 'local_x')`의 키에서 F12 → `settings.php`의 `admin_setting_*` 선언, hover(설정 클래스·선언 위치), 해석 키 하이라이팅(`csmscode.config.highlightResolved`), 코드↔선언 양방향 Shift+F12, `settings.php` 선언 줄 위 "사용 N건" 버튼(`csmscode.config.codeLens`)과 hover 아래 "사용 N건 보기" 링크, `get_config('local_x', '|')` 키 완성. 플러그인이 `$this->pluginname`·`$var`·`Class::CONST`인 호출도 문자열과 같은 전파로 해석합니다.
  - 선언은 `$pluginname = 'local_x'; $name = $pluginname . '/key'; new admin_setting_configtext($name, …)` 관용구를 등장 순서대로 따라가 읽습니다(리터럴·`$name` 변수·연결·보간·코어 특수 설정의 `parent::__construct`·복수형 클래스명 포함). 실측(hlulxp 커스텀 코드) `get_config('p','k')` 427건 중 **383건(89.7%)** 이 선언으로 해석됩니다 — 리터럴 선언만 보면 16%였습니다.
  - 플러그인 이름은 저장 키 그대로 비교합니다(`ubboard`≠`mod_ubboard` — `config_plugins`의 다른 행). `''`·`moodle`·`core`만 core로 접습니다.
- **선언 색인은 첫 요청에서 만들고(활성화 비용 0) `$CFG->` 전역 색인과 공유합니다.** `settings.php`를 열거나(`config.codeLens`가 켜져 있을 때), 설정 호출이 있는 문서를 열거나(`config.highlightResolved`가 켜져 있을 때), 설정 키에서 F12·hover·Shift+F12·완성을 쓰면 만들어집니다. 주석 처리된 선언은 무시합니다.

### 수정
- **`settings.php`를 저장하면 선언 색인이 그 파일만 즉시 갱신됩니다.** 전역 색인 중 설정 키가 처음으로 세션 중 갱신되는 색인이 됐습니다(클래스 멤버 색인은 여전히 첫 요청 한 번).

### 비고
- 사용처 목록·개수에는 플러그인이 리터럴인 호출만 들어갑니다(사용처 색인이 정규식 기반 — 문자열과 같은 제한). 값에 괄호가 든 `set_config('k', get_config(…), 'p')`는 어디서 끝나는지 정규식으로 알 수 없어 목록에서 빠집니다.
- 비목표(백로그): 코어 키(`$CFG->x` 18,894건 — 사용처 색인이 크게 무거워지고 커스텀 코드의 `get_config('core', …)`는 0건), `$c = get_config('local_x'); $c->key` 통째 접근(72건), 누락 키 진단(런타임 전용 키 때문에 오탐 필연), hover에 설정 제목·기본값 표시.
- 문자열용이던 참조 프로바이더·CodeLens·hover 링크·명령을 대상 종류(string/config)에 일반화했습니다 — 라벨·명령 이름은 그대로입니다.

## [0.16.0] — 2026-08-26

### 추가
- **문자열 호출 가족 확장.** `get_string`·`print_string`에 더해 다음이 같은 인텔리전스(완성·정의 이동·hover·하이라이트·누락 키 진단·사용처 참조·"사용 N건")를 받습니다: `print_error('code', 'comp')`, `new moodle_exception('code', 'comp')`(`\moodle_exception` 접두 포함), `new lang_string('key', 'comp')`, `new help_icon('key', 'comp')`. 형태 표(`string-functions.ts`) 한 곳에 이름과 기본 컴포넌트를 두고 팩트 추출·사용처 색인·완성 트리거가 공유합니다.
- **컴포넌트를 생략한 한 인자 호출을 인식합니다.** `get_string('ok')`는 core(`lang/en/moodle.php`)로, `print_error`·`moodle_exception`은 컴포넌트 생략·`moodle`·`core`일 때 **`error`**(`lang/en/error.php`)로 갑니다 — Moodle의 `moodle_exception` 규칙 그대로입니다.
  - 실측(hlulxp 커스텀 코드): 한 인자 `get_string` 534건 중 core에 없는 키 5건(0.9%). 한 인자 `moodle_exception`/`print_error` 172건 중 `error.php`에 없는 코드 **118건(68.6%)** — 이 코드들은 런타임에 `[[code]]`로 표시되므로 새로 붙는 경고는 실제 결함입니다.
- 키 완성이 한 인자 꼴(`new moodle_exception('|')`)에서도 뜹니다 — 그 형태의 기본 컴포넌트(error/core) 키를 제안합니다. 컴포넌트도 닫는 괄호도 아직 없으면 전처럼 제안하지 않습니다.

### 수정
- 사용처 색인이 `get_string('k', $comp)`처럼 컴포넌트가 변수인 호출을 core로 잘못 귀속시키던 것을 고쳤습니다 — 이제 팩트 쪽과 같이 침묵합니다.
- 누락 키 진단 메시지가 호출에 적힌 이름 대신 lang 파일의 컴포넌트(canonical — `core_error`, `mod_testmod`)를 씁니다.

### 비고
- 실측 규모: 커스텀 코드 `get_string` 8,064건 대비 새로 들어온 형태는 약 240건(`moodle_exception` 164·`print_error` 39·`lang_string` 22·`help_icon` 8). 이름은 많지만 호출은 적습니다.
- 비목표(백로그): 키가 2번째 인자인 `->addHelpButton($el, 'k', 'c')`(20건), `->help_icon()`·`get_string_manager()->…` 메서드 호출(11건), `get_strings([...])`, `new` 꼴의 동적 컴포넌트(`new moodle_exception('k', $this->component)` 10건).

## [0.15.0] — 2026-08-25

### 추가
- **코드에서도 Shift+F12로 문자열 사용처를 찾습니다.** `get_string('key', 'comp')`의 키 위에서(PHP·`amd/src` JS·mustache `{{#str}}`) 참조를 찾으면 그 문자열을 쓰는 모든 호출처가 lang 정의(ko·en)와 함께 나옵니다 — 다른 언어 지원처럼 선언도 참조 목록에 포함됩니다. 전에는 lang 파일에서만 되어서 F12는 되는데 Shift+F12는 안 되는 것처럼 보였습니다.
- **lang 파일에 "사용 N건" 버튼(CodeLens).** `$string['key']` 줄 위에 버튼이 붙고, 클릭하면 그 자리에서 사용처 목록(peek)이 열립니다. 사용처 색인이 아직 없으면 "사용 찾기"로 표시되고 클릭이 색인을 만듭니다 — 파일을 여는 것만으로는 워크스페이스를 스캔하지 않습니다. 개수는 화면에 보이는 줄에만 계산합니다. 설정 `csmscode.strings.codeLens`(기본 `true`).
- **hover 아래 "사용 N건 보기" 링크.** 코드 쪽 문자열 hover(PHP·JS·mustache) 맨 아래에서 클릭하면 같은 목록이 열립니다. 링크가 부르는 명령 하나만 신뢰하므로 lang 값 텍스트 안의 다른 링크는 실행되지 않습니다.
- **`print_string`도 인식합니다.** `get_string`과 같은 `(key, component)` 시그니처라 완성·정의 이동·hover·하이라이트·누락 키 진단·사용처 참조가 모두 동작하고, 컴포넌트가 변수·프로퍼티·상수인 호출도 같은 전파로 해석됩니다. 함수 이름 집합을 한 곳에 두어 팩트 추출·사용처 색인·완성 트리거가 갈라질 수 없습니다.

### 수정
- 키를 겹따옴표로 쓴 lang 줄(`$string["key"]`)에서도 Shift+F12가 동작합니다. 0.14.0에서 파서는 겹따옴표를 읽게 됐지만 커서 위치 판정이 홑따옴표만 봐서 F12는 되는데 역참조만 안 됐습니다.

### 비고
- 사용처 목록과 버튼의 개수는 여전히 **리터럴 컴포넌트 호출만** 셉니다(사용처 색인이 정규식 기반). `get_string('k', $this->pluginname)` 같은 호출은 F12·hover는 되지만 목록·개수에는 없습니다 — 백로그 "동적 컴포넌트 호출은 사용처 목록·개수에 안 나온다" 참고.
- 참조 프로바이더 여섯 개(lang·템플릿·AMD·코드 쪽 세 표면)와 버튼·링크가 사용처 색인 핸들 하나를 공유합니다 — 첫 요청에서 한 번만 만들고 이후 저장 단위로 증분되며, 빌드 중에 겹친 요청은 같은 진행률 알림을 기다립니다.

## [0.14.0] — 2026-08-11

### 추가
- **`get_string`의 컴포넌트가 리터럴이 아니어도 추적합니다.** 같은 파일의 문자열 리터럴까지 한 단계 거슬러 올라가 해석하고, 해석되면 정의 이동·hover·하이라이트·누락 키 진단이 모두 동작합니다.
  - `$comp` ← 같은 스코프의 리터럴 대입
  - `$this->pluginname` ← 프로퍼티 선언 기본값
  - `Class::NAME` ← const 선언
- **서로 다른 리터럴이 둘 이상이면 해석하지 않습니다**(침묵). 틀린 컴포넌트로 해석하면 누락 키 진단이 곧 오탐이 되기 때문입니다 — 실측 모호 사례는 0건이라 이 가드로 잃는 커버리지가 없습니다.
- 실측(hlulxp 커스텀 코드): 동적 컴포넌트 호출 2,406건 중 **2,081건(86.5%)** 해석. 리터럴 호출 3,014건과 합쳐 문자열 표면이 3,014 → 5,095건으로 늘었습니다.

### 수정
- **lang 값 파서가 겹따옴표와 연결 연산자를 읽습니다.** 전에는 `$string['k'] = "값";`과 `'a' . $string['b'] . 'c'` 형태가 색인에 없어 그 키를 쓰는 곳마다 "문자열이 없습니다" 경고가 났습니다. 실측 (컴포넌트/키) 41,149 → 41,298(+149, 잃은 것 0).
  - 연결 안의 비리터럴 조각은 `…`로 표시하고 키는 정의된 것으로 봅니다.
  - 홑따옴표 값 안의 겹따옴표(실측 510건)를 잃지 않도록 델리미터별로 문자 클래스를 갈랐습니다.

### 비고
- 전파로 새로 붙는 경고는 hlulxp 커스텀 코드에서 28건이고, 확인해보니 **전부 실제로 lang에 없는 키**입니다(예: `local_manager/delete_complete`는 `local_ubion`에만 있어 실행 시 `[[delete_complete]]`로 표시됩니다). 파서 한계로 인한 오탐 6건은 위 수정으로 사라졌습니다.
- 동적 컴포넌트 호출은 lang 파일의 Shift+F12 역참조 목록에는 나오지 않습니다(사용처 색인이 정규식 기반).

## [0.13.0] — 2026-08-11

### 추가
- **PHP에서 `$`가 단어에 포함됩니다.** 더블클릭·Ctrl+D가 `$config`를 통째로 선택합니다(VS Code 기본은 `$`를 구분자로 봐서 `config`만 선택). 프로퍼티 접근(`$config->courseid`)이나 호출 구두점에서는 기존과 똑같이 갈립니다.
- 설정 `csmscode.php.selectDollarInWord`(기본 `true`). 끄면 등록을 해제해 기본 동작으로 즉시 돌아갑니다(창 재시작 불필요).
- 다른 PHP 확장이 나중에 같은 언어 설정을 등록하면 그쪽이 이길 수 있습니다 — 등록 순서는 이 확장이 통제할 수 없습니다.

## [0.12.0] — 2026-08-11

### 추가
- **`$DB` 메서드의 테이블 인자도 링크됩니다.** `$DB->update_record('local_x', $data)`·`get_record`·`delete_records`·`count_records` 등의 첫 문자열 인자에서 F12로 `install.xml`의 `<TABLE>` 줄로 이동하고, 해석되는 것은 링크 색상으로 표시됩니다(설정은 기존 `csmscode.tables.highlightResolved`를 그대로 씁니다).
- SQL 문자열의 `{table}`과 **같은 팩트**로 담기므로 정의 이동·하이라이트가 한 경로를 탑니다.
- `$DB->sql_like('email', …)`처럼 `sql_` 계열 헬퍼는 첫 인자가 컬럼·식이라 제외합니다 — 실측 `block_rss_client`·`feedback` 두 건이 테이블 이름과 겹쳐 잘못 링크될 뻔했습니다.
- 실측(hlulxp 커스텀 코드): 리터럴 첫 인자 1,150건 중 **91.3%** 해석. 나머지는 컬럼 이름이거나 이 체크아웃에 install.xml이 없는 테이블입니다.

## [0.11.0] — 2026-08-10

### 추가
- **Mustache 내부 인텔리전스**. `.mustache` 파일은 지금까지 "바깥에서 자기를 부르는 PHP·JS 찾기"만 됐고 파일 안의 내용은 해석하지 않았습니다.
  - `{{> comp/name}}`·`{{< comp/name}}`에서 **F12로 그 템플릿 파일로 이동**(테마 오버라이드가 있으면 함께), hover, 해석 참조 하이라이팅.
  - `{{#str}}key, component{{/str}}`·`{{#cleanstr}}`에서 **lang 파일로 이동**하고 hover로 한국어·영어 값을 봅니다. 컴포넌트 정규화는 PHP 경로와 같은 규칙입니다.
  - **템플릿의 Shift+F12 사용처 목록에 다른 mustache의 partial 참조가 포함**됩니다. lang 파일의 Shift+F12에도 mustache의 `{{#str}}` 사용처가 나옵니다.
  - 실측(hlulxp, 템플릿 2,952개): partial·parent 참조 3,215건 중 **98.8%**, `{{#str}}` 8,138건 중 **99.2%** 해석.
- **코어 서브시스템 템플릿**을 색인합니다 — `grade/templates`·`lib/form/templates`·`message/templates` 등. 컴포넌트 역산 커버리지가 실측 **85.0% → 100.0%**(2,952/2,953)로 올라, `render_from_template('core_grades/status_icons')` F12도 이제 동작합니다.

### 비고
- `.mustache`의 languageId는 어떤 확장을 깔았는지에 따라 달라(`mustache`·`html`·`plaintext`) 신뢰할 수 없으므로, 프로바이더와 하이라이트를 **경로 기준**으로 고릅니다.
- mustache에는 진단을 붙이지 않습니다(인자가 변수인 형태와 조건부 블록의 오탐 위험을 따로 재야 합니다).

## [0.10.0] — 2026-08-07

### 수정
- **진단 코드가 종류를 담습니다.** 언어 문자열 진단이 `csms.column`으로 표시돼 Problems 패널에서 컬럼 진단처럼 보였습니다(`'local_ubattendance'에 'grade_export' 문자열이 없습니다. csms.column`). 이제 컬럼은 `csms.column`, 문자열은 `csms.string`이고 제안이 있으면 `csms.<종류>.<제안>`입니다.
- **출처 이름을 하나로 통일했습니다.** 진단은 `CSMS Code`, 자동완성·hover는 `csms-intelli`로 갈려 있었습니다. 모두 `csms-intelli`입니다.
- QuickFix는 진단 종류와 무관하게 제안을 읽습니다 — 코드 이름이 바뀌어도 문자열 제안 고치기가 사라지지 않도록 해석을 한 곳으로 모으고 테스트로 고정했습니다. 프로바이더명도 `SuggestionQuickFixProvider`로 바꿨습니다.
- 확장 설명이 "DB 레코드 컬럼 인텔리전스"에 멈춰 있어 현재 기능 범위로 고쳤습니다.

## [0.9.0] — 2026-08-07

### 추가
- **출처 표시**. 자동완성 항목의 오른쪽 끝과 hover 아래에 `csms-intelli`가 붙습니다 — 다른 PHP 확장이 준 결과와 구분하기 위한 것입니다. 필터링에 쓰이는 라벨 자체는 바뀌지 않습니다.
- **상태 표시줄**. 화면 아래에 `csms-intelli`와 색인된 테이블 수가 표시되고, 색인 중에는 회전 아이콘으로 바뀝니다. 마우스를 올리면 Moodle 루트 경로와 테이블·언어 문자열·템플릿·AMD 모듈 색인 수가 나옵니다.
- 설정 `csmscode.showSourceLabel`(기본 `true`)로 완성·hover의 표시를 끌 수 있습니다. 상태 표시줄은 설정과 무관하게 남습니다.
- 색인이 실패하면 상태 표시줄이 오류 아이콘과 사유를 보여줍니다 — 확인 수단이 실패를 성공처럼 보이면 안 됩니다.
- 파일 저장으로 색인이 증분 갱신되면 상태 표시줄 숫자도 함께 움직입니다.

## [0.8.1] — 2026-08-07

### 문서
- 추론 범위 설명을 정정했습니다. "현재 함수/메서드 스코프 내"라고 적혀 있어 최상위 스크립트에서는 동작하지 않는 것처럼 읽혔지만, 실제로는 함수 밖 최상위 코드(Moodle 페이지의 일반적인 모양)에서도 파일 전체를 하나의 스코프로 보고 똑같이 동작합니다 — `?>` HTML `<?php`로 끊긴 뒤에도, 재대입 후 침묵도 그대로입니다. 이 동작을 회귀 테스트로 고정했습니다.

## [0.8.0] — 2026-08-07

### 추가
- **Moodle 전역 인텔리전스**. `global $DB, $CFG, $USER;` 선언에는 타입 정보가 없어 일반 PHP 언어 서버가 아무것도 주지 못하던 자리를 채웁니다.
  - `$DB->`·`$PAGE->`·`$OUTPUT->` — 코어 클래스(`moodle_database`·`moodle_page`·`core_renderer`)의 public 멤버를 인자 시그니처·설명과 함께 완성하고, hover와 F12로 코어 소스의 선언 줄까지 갑니다. `$PAGE->context`처럼 `magic_get_*`로 구현된 매직 프로퍼티도 포함합니다(실측 41개).
  - `$CFG->` — `config-dist.php`와 플러그인 `settings.php`의 `admin_setting_*` 선언에서 모은 설정 키를 완성하고 선언 위치로 이동합니다(실측 1,622개).
  - `$USER->`·`$COURSE->`·`$SITE->` — `user`·`course` 테이블 컬럼을 기존 컬럼 엔진으로 완성·hover·이동합니다.
  - 전역이 지역 변수로 가려지면(`foreach ($users as $USER)`, 재대입) 전역 경로가 빠지고 기존 레코드 추론이 담당합니다.
  - `$DB` 완성은 자주 쓰는 메서드(`get_record`·`get_records_sql` 등)를 위로 올립니다 — `moodle_database`만 실측 126개입니다.

실측(hlulxp 4.5.8): `$DB` 접근 2,768회의 이름 47개 중 46개가 `moodle_database`에 선언돼 있고, `$CFG` 접근 1,627회 중 약 73%가 선언에서 확인됩니다.

### 성능
- 전역 색인은 **첫 요청에서 한 번** 만듭니다 — 실측(웜 2회) 코어 클래스 파싱 79~86ms·최대 이벤트 루프 정지 29~33ms, 설정 키 수집 192~215ms·최대 정지 17~18ms. 둘 다 한 자릿수 ms 목표를 넘겨 활성화 경로에 넣을 수 없습니다. 활성화 자체는 그대로입니다(전체 빌드 762ms·최대 정지 8ms).

### 비고
- 전역에는 진단을 붙이지 않습니다. `$USER`는 사이트가 주입하는 런타임 필드가 실측 81건, `$CFG`는 `set_config`로 만들어지는 키가 실측 431회분이라 경고가 그대로 오탐이 됩니다.

## [0.7.0] — 2026-08-07

### 변경
- **플러그인 타입 매핑을 Moodle 선언에서 읽습니다**. 손으로 관리하던 30개 상수 대신 `lib/components.json`의 `plugintypes`와 각 플러그인의 `db/subplugins.json`(구버전은 `db/subplugins.php`)을 합쳐 구성합니다. 선언이 없는 버전에서는 기존 상수가 폴백으로 남아 커버리지가 줄지 않습니다.
- 그동안 어떤 기능에서도 보이지 않던 플러그인들이 색인됩니다 — `qbank`·`qbehaviour`·`qformat`·`quizaccess`·`assignsubmission`·`assignfeedback`·`tiny`·`gradingform`·`dataformat`·`media` 등.

실측(hlulxp 4.5.8):

| | 전 | 후 |
|---|---|---|
| 플러그인 타입 | 30 | **65** |
| install.xml(테이블) | 99 (657) | 126 (712) |
| lang 파일 | 497 | 645 |
| 템플릿 | 2,400 | 2,510 |
| AMD 모듈 | 1,034 | 1,201 |

구버전도 `subplugins.php` 해석만으로 늘어납니다 — 2.9.4에서 타입 30 → 43, install.xml 71 → 87, lang 433 → 483.

### 성능
- 타입 맵은 루트별로 캐시하고 활성화 시 비동기로 미리 채웁니다(실측 구성 85ms, 그중 최대 이벤트 루프 정지 1ms). 경로 역산이 워처 이벤트·정의 이동마다 이 맵을 쓰므로 캐시 미스가 확장 호스트를 막지 않아야 합니다.
- 색인 대상이 늘어 활성화 전체 빌드 wall-clock이 실측 597ms → 714ms(+20%)입니다. 최대 이벤트 루프 정지는 7ms로 같습니다.
- `coreSubsystemDirs`가 호출마다 하던 `lib/components.json` 읽기가 같은 캐시로 흡수됐습니다.

## [0.6.0] — 2026-08-06

### 추가
- **AMD 모듈 참조 이동**: `$PAGE->requires->js_call_amd('local_ubion/user', 'index')`의 첫 인자에서 F12를 누르면 `local/ubion/amd/src/user.js`로 이동합니다. 중첩 경로(`local_x/foo/bar`)와 코어 서브시스템(`core_form/submit` → `lib/form/amd/src/submit.js`)도 해석하며, 실측 해석률은 추출된 338건 중 328건(97.0%)입니다. 정규식으로 세면 354건이지만 차이 16건은 겹따옴표 리터럴 2건과 주석 처리된 호출이라 AST 기준에서 제외된 것입니다.
- `amd/src`의 모듈 파일에서 Shift+F12 → 그 모듈을 부르는 `js_call_amd` 호출처 목록.
- 해석되는 모듈 참조 하이라이팅. 설정 `csmscode.amd.highlightResolved`(기본 `true`).
- 코어 서브시스템 디렉터리 매핑을 `lib/components.json`에서 읽습니다. 이 파일이 없는 구버전에서는 코어 서브시스템 모듈만 해석되지 않고 플러그인 모듈은 그대로 동작합니다.

## [0.5.0] — 2026-08-06

### 추가
- **SQL 테이블 참조 이동**: PHP 문자열 안의 `{tablename}`에서 F12를 누르면 그 테이블을 선언한 `install.xml`의 `<TABLE>` 줄로 이동합니다. 단일 인용·이중 인용·heredoc·nowdoc을 모두 지원하고, `{$var}` 보간은 대상이 아닙니다.
- 해석되는 테이블 참조를 링크 색상으로 하이라이팅합니다. 설정 `csmscode.tables.highlightResolved`(기본 `true`)로 끌 수 있습니다.
- `install.xml`에 없는 이름(정규식 수량자 `{4}`, 다른 템플릿 문법 `{Bucket}` 등)에는 이동·하이라이트·진단 모두 반응하지 않습니다. 실측상 문자열 내 `{이름}` 중 실제 테이블은 57.8%뿐이라 진단은 제공하지 않습니다.

### 변경
- `csmscode.detectInSubfolders` 기본값이 `[]` → `["moodle"]`입니다. Moodle을 `moodle/` 하위에 두는 배치가 흔해, 이전에는 그런 워크스페이스에서 설정을 넣기 전까지 확장이 아무것도 하지 않았습니다. `version.php`와 `lib/db/install.xml`이 함께 있을 때만 루트로 인정하므로 이름만 같은 디렉터리를 잘못 잡지 않습니다.

### 수정
- PHP 파싱이 실패한 뒤 **다음 문서**가 예외 없이 노드가 누락된 트리로 파싱되던 문제. 이 tree-sitter 조합은 16진 이스케이프(`"\x00"`)가 있는 파일에서 파싱이 실패하는데, 중단된 파싱이 파서에 상태를 남겼습니다. 실패 즉시 파서 상태를 버리고 그 파일만 침묵합니다(잘못된 팩트는 진단 오탐으로 이어질 수 있어 침묵보다 위험합니다).

## [0.4.0] — 2026-08-06

### 성능
- **활성화 색인 비동기화**: install.xml·lang·템플릿 3종 색인의 파일 열거와 읽기를 모두 `fs.promises`로 바꾸고 200항목마다 이벤트 루프를 양보합니다. 실측 최대 확장 호스트 블로킹 콜드 ~1,730ms → **5.8ms**. 진행 상황은 상태바(`ProgressLocation.Window`)에 표시합니다.
- **워처 파일 단위 증분**: 파일 하나 저장에 전체 재색인하던 동작을 해당 파일만 갱신하도록 바꿨습니다. lang 저장 실측 122ms → ~6ms(최악 ~10ms), install.xml·템플릿은 1ms 미만.
- 세 색인의 동기 빌드·비동기 빌드·증분 세 경로가 **조립 함수 하나**를 통과하도록 구조를 고정하고, 등가성·수렴 테스트로 핀을 박았습니다.
- 워처가 폭주할 때 하이라이트·진단 갱신을 200ms로 합칩니다.

### 수정
- 템플릿 증분 갱신이 위치 배열 순서를 보존합니다 — 원본 파일 저장 후 F12·hover의 표시 순서가 뒤바뀌던 문제.
- 전체 재빌드 게이트를 예외 안전(`try/finally`)·반복형(`do/while` catch-up)으로 통합했습니다. 이전에는 빌드 중 예외가 나면 이후 모든 증분 동기화가 조용히 멈췄습니다.
- 역산이 실패한 경로에 대한 폴백 전체 재빌드를 제거했습니다(열거가 역산과 같은 규칙을 쓰므로 색인될 수 없어 이득이 없고, 진행 중 증분을 덮어쓸 위험만 있었습니다).

## [0.3.0] — 2026-08-05

### 추가
- **JS/AMD 인텔리전스**: `amd/src`의 `get_string`(`M.util.get_string`·`core/str` 양쪽)과 `Templates.render`/`renderForPromise` 리터럴에 정의 이동·hover·하이라이팅을 제공하고, lang·템플릿 참조 목록(Shift+F12)에 JS 호출처를 포함합니다. 생성물인 `amd/build`·`*.min.js`는 색인에서 제외합니다.
- JS 하이라이트를 `csmscode.strings.highlightResolved`·`csmscode.templates.highlightResolved` 설정별로 각각 라우팅합니다.

## [0.2.0] — 2026-08-05

### 추가
- **언어 문자열 인텔리전스**: `get_string('key', 'component')` 키 자동완성(한국어 값 미리보기)·정의로 이동(ko/en)·hover·누락 키 진단.
- **lang 파일 → 사용처 참조 이동**(Shift+F12) 및 해석되는 키 하이라이팅. 사용처 색인은 첫 요청 시 lazy 구축 + 저장 단위 증분.
- **Mustache 템플릿 인텔리전스**: `render_from_template('component/name', …)`에서 `.mustache`로 이동(테마 오버라이드 함께 표시)·템플릿 파일에서 사용처 참조 이동·해석 참조 하이라이팅.
- 설정 `csmscode.strings.highlightResolved`, `csmscode.templates.highlightResolved`.

### 수정
- **플러그인 타입 → 디렉터리 매핑**: `block`→`blocks`, `tool`→`admin/tool`, `qtype`→`question/type` 등 실제 디렉터리와 다른 20여 개 타입이 색인되지 않던 문제를 검증된 30개 매핑으로 고쳤습니다.
- **kill-on-reassign 추론**: 레코드 변수를 다른 값으로 재대입한 뒤의 오탐 경고 제거 + foreach 바인딩이 가려지던 문제.
- **진단 debounce + 팩트 캐시**: 문서별 300ms debounce와 텍스트 키 LRU 캐시(용량 8)로 타이핑 중 중복 파싱 제거.
- 컬렉션 메서드(`get_records`·`get_recordset` 등) 결과 변수 자체에 컬럼을 바인딩하지 않도록 정밀화 — 레코드는 foreach 항목 변수입니다.
- 심볼릭 링크된 플러그인 디렉터리 색인, `scopeContaining`의 클로저 바깥 바인딩 누수, 키 위치 계산의 접두부 충돌 버그.

## [0.1.0] — 2026-07-27

### 추가
- **DB 레코드 컬럼 인텔리전스**(Phase 1): `db/install.xml` 색인을 바탕으로 `$DB->get_record()`·`get_records()` + `foreach`·`insert_record`/`update_record` 인자·`@var` 주석으로 바인딩된 `stdClass` 변수의 컬럼 자동완성(한국어 `COMMENT` 포함)·정의로 이동·hover·오타 진단 + Quick Fix.
- Moodle 루트 자동 인식(코어 + 커스텀 플러그인), 파일 변경 감지 재색인.
- 설정 `csmscode.detectInSubfolders`, `csmscode.diagnostics.enable`.

---

0.1.0~0.4.0 항목은 2026-08-06에 커밋 기록(`9134bb2`…`c6e817a`)에서 역산해 정리했습니다.
버전 태그는 소급 생성하지 않았습니다.
